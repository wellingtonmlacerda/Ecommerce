@model Ecommerce.Models.Produto_CategoriaModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Produto_CategoriaModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="PRCA_DESCRICAO" class="control-label"></label>
                <input asp-for="PRCA_DESCRICAO" class="form-control" />
                <span asp-validation-for="PRCA_DESCRICAO" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PRCA_STATUS" class="control-label"></label>
                <select asp-for="PRCA_STATUS" asp-items="Html.GetEnumSelectList<Produto_CategoriaModel.StatusCadastro>()" class="form-control"></select>
                <span asp-validation-for="PRCA_STATUS" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
